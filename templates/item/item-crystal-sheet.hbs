<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header sheet-min">
        <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
        <div class="header-fields">
            <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name" /></h1>
        </div>
    </header>

    <!-- Sheet Tab Navigation -->
    <nav class="sheet-tabs tabs" data-group="primary">
        <a class="item" data-tab="properties">Propriétés</a>
        <a class="item" data-tab="description">Détail du pouvoir</a>
    </nav>

    <!-- Sheet Body -->
    <section class="sheet-body">
        <!-- Properties Tab -->
        <div class="tab properties" data-group="primary" data-tab="properties">
            <div class="grid grid-2col">
                <div class="flexrow flex-group-center">
                    <label for="system.source" class="title-up-label">Source</label>
                    <select name="system.source">
                        {{#select system.source }}
                        {{#each system.sources }}
                        <option value="{{this}}">{{this}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </div>
                <div class="flexrow flex-group-center">
                    <label for="system.family" class="title-up-label">Famille</label>
                    <select name="system.family">
                        {{#select system.family }}
                        {{#each families }}
                        <option value="{{this}}">{{this}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </div>
            </div>

            <div class="grid grid-1col">
                <div class="flexrow flex-group-center">
                    Le rang d'un pouvoir indique sa puissance
                    <br>et souvent la difficulté qui sera nécessaire pour l'utiliser.
                    <br>Rang : 1/ 2 / 3 / 4
                    <br>Difficulté : Simple / Standard / Complexe / Légendaire
                </div>
            </div>

            <div class="grid grid-2col">
                <div class="flexrow flex-group-center">
                    <label class="title-up-label">Rang</label>
                    <input type="text" name="system.rank" value="{{system.rank}}" data-dtype="Number" />
                </div>
                <div class="flexrow flex-group-center">
                    {{#if (eq system.chaneling true )}}Avec canalisation{{/if}}
                    {{#if (eq system.chaneling false )}}Sans canalisation{{/if}}
                </div>
            </div>

            <div class="grid grid-2col">
                <div class="flexrow flex-group-center">
                    <label for="system.duration" class="title-up-label">Durée max</label>
                    <select name="system.duration">
                        {{#select system.duration }}
                        {{#each durations }}
                        <option value="{{this}}">{{this}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </div>
                <div class="flexrow flex-group-center">
                    <label for="system.range" class="title-up-label">Portée max</label>
                    <select name="system.range">
                        {{#select system.range }}
                        {{#each ranges }}
                        <option value="{{this}}">{{this}}</option>
                        {{/each}}
                        {{/select}}
                    </select>
                </div>
            </div>
        </div>

        <!-- Description Tab -->
        <div class="tab" data-group="primary" data-tab="description">
            {{
                editor
                enrichedBiography
                target="system.description"
                editable=editable
                button=true
            }}
        </div>

    </section>
</form>